# Plantilla de variables de entorno.
# Copia este archivo a ".env" y pon tu contraseña real de postgres.
# El archivo .env NO se sube al repositorio (está en .gitignore); este .env.example sí (es una plantilla, sin secretos).

DATABASE_URL=postgresql+psycopg2://postgres:TU_CONTRASEÑA@localhost:5432/diagnostico

# Secreto para firmar los tokens JWT. Debe ser una cadena larga y aleatoria.
# Genera el tuyo con:  python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=CAMBIA_ESTO_por_una_cadena_larga_y_aleatoria

# Contraseña del usuario ADMIN que crea el seed (python -m app.seed).
# Elige la tuya; solo va en tu .env local (no se sube al repositorio).
ADMIN_PASSWORD=elige_una_contraseña

# Orígenes (URLs) del frontend autorizados a llamar a la API (CORS).
# Admite varios separados por comas, para que convivan el entorno local y el desplegado:
#   FRONTEND_ORIGINS=http://localhost:5173,https://mi-frontend.onrender.com
# Sin barra final. Si no se define, se usa http://localhost:5173.
FRONTEND_ORIGINS=http://localhost:5173
